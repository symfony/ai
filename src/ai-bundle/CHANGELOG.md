CHANGELOG
=========

0.2
----------

 * [BC BREAK] Remove "Agent" and "MultiAgent" suffixes from injection aliases. Agent and MultiAgent services are now injected using their configuration name directly (e.g., `$blog` instead of `$blogAgent`, `$support` instead of `$supportMultiAgent`).

0.1
---

 * Add the bundle
