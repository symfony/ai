CHANGELOG
=========

0.9
---

 * Add the bridge with chat completions, embeddings, text-to-speech,
   speech-to-text and image generation support over the Bifrost OpenAI-compatible
   HTTP gateway, including a dynamic model catalogue backed by `GET /v1/models`.
