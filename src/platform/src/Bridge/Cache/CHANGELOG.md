CHANGELOG
=========

0.10
----

 * `CachePlatform` now caches inputs implementing `CacheableInputInterface` (e.g. `DocumentUrl`, `ImageUrl`, `File`/`Audio`), so document, OCR and audio-transcription tasks can be cached, not just `string`/`array`/`MessageBag` inputs

0.3
---

 * Add the bridge
