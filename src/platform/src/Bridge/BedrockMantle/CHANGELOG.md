CHANGELOG
=========

0.11
----

 * Add the bridge, supporting the OpenAI-compatible AWS Bedrock Mantle endpoint (Chat Completions via `Factory` and the Responses API via `Responses\Factory`), with both Bedrock API key (bearer token) and AWS SigV4 authentication
